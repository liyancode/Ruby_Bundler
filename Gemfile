source 'https://rubygems.org'      # gem源地址
gem 'nokogiri'                     # 被依赖的gem，不指定版本默认安装最新版本
gem 'rack', '~>1.1'                # 指定版本
gem 'rspec', :require => 'spec'    # gem 名称和主文件名不一致，要指定require的主文件